sudo yum update 
sudo yum install nginx # install nginx
sudo nginx # start nginx
sudo nginx -s reload # command to reload the nginx.conf after making changes to it
# config files at /etc/nginx/nginx.conf
# initial html.index file at /usr/share/nginx/html/index.html
